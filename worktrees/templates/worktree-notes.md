# Git Worktree Notes — project-specific calibrations

## Repo conventions
- Default base branch for new worktrees: <branch>
- Line-ending / autocrlf policy: <note>
- Per-module install command (if needed after worktree create): <cmd>
- IDE indexer behavior in worktrees: <note>
- Graphify index present at repo root: <yes/no>

## Known traps

(append numbered sections as they are discovered during worktree work)
