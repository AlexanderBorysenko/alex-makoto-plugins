---
description: Open or resume a budgeted bug investigation — case file, hypothesis ledger, cheapest-test-first loop, verdict dossier with map tour.
---

Invoke the `investigate` skill on this symptom:

$ARGUMENTS

If a case for this symptom already exists under `.claude-memory/cases/`, resume it instead of opening a duplicate. Apply the L1 fast path first: an obvious culprit with evidence in hand gets a direct answer, not a case.
