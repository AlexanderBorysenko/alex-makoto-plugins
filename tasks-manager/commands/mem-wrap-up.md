---
description: Deprecated alias for /task-wrap-up (plugin renamed memory-system → tasks-manager).
---

Deprecated alias. Run the same workflow as `/task-wrap-up`: invoke the `tasks-manager` skill and execute the **Wrap-up protocol** per the /task-wrap-up steps. Mention once that this command is now `/task-wrap-up`.
