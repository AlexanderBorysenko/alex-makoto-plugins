---
description: Deprecated alias for /task-start (plugin renamed memory-system → tasks-manager).
---

Deprecated alias. Run the same workflow as `/task-start`: invoke the `tasks-manager` skill and execute the **Startup ritual** section. Mention once that this command is now `/task-start`.
