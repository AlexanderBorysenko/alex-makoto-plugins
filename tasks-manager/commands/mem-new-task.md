---
description: Deprecated alias for /task-new (plugin renamed memory-system → tasks-manager).
argument-hint: <slug-or-description>
---

Deprecated alias. Run the same workflow as `/task-new` with arguments `$ARGUMENTS`: invoke the `tasks-manager` skill and create a new per-task journal per the /task-new steps. Mention once that this command is now `/task-new`.
