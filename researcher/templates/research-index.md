# Research index

One line per finding: `- [Title](findings/slug.md) — <level> — <YYYY-MM-DD>`.
Compiled topic pages live in `wiki/` and are listed by the index CLI, not here.
Regenerate the staleness view (findings + wiki pages) with the researcher
plugin's index CLI: `node <plugin root>/bin/research-index.js list`
