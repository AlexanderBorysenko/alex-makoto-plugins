{
  "$comment": "Role → model map for ticket-resolver. The orchestrator reads this and passes the model when dispatching each agent; if a role is omitted, the agent's own frontmatter model: is used as the default. Edit to retune the per-phase cost/quality split (e.g. set implementer to haiku) with no code change. Valid values: opus | sonnet | haiku.",
  "orchestrator": "opus",
  "analyzer": "opus",
  "validator": "haiku",
  "repro": "sonnet",
  "implementer": "sonnet",
  "reviewer": "opus",
  "verifier": "sonnet"
}
